# HTML

Standalone HTML snippets and templates. See subfolders for `snippets/` (small reusable fragments) and `templates/` (full page/document starting points).

Governance: see `/GOVERNANCE.md`, `/CONTRIBUTING.md`, `/TAGS.md`, `/AI-DISCLOSURE.md`, and `/LICENSE` at the repo root (pending — tracked in `BACKLOG.md`).
