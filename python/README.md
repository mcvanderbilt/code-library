# Python

Python snippets and functions. Learning-stage Python code lives separately under the repo-root `/learning/` folder (not synced to GitHub), so content here is held to the same professional quality bar as the other language folders.

Governance: see `/GOVERNANCE.md`, `/CONTRIBUTING.md`, `/TAGS.md`, `/AI-DISCLOSURE.md`, and `/LICENSE` at the repo root (pending — tracked in `BACKLOG.md`).
