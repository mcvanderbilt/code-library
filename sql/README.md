# SQL

SQL snippets, organized by task rather than dialect: `tables/`, `views/`, `stored-procedures/`, `functions/`, `queries/`, `triggers/`, `indexes/`. A dialect split may be added later under each task folder if needed (see `BACKLOG.md`).

Governance: see `/GOVERNANCE.md`, `/CONTRIBUTING.md`, `/TAGS.md`, `/AI-DISCLOSURE.md`, and `/LICENSE` at the repo root (pending — tracked in `BACKLOG.md`).
