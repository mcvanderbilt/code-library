# Staging

Larger, self-contained projects temporarily housed in this repo before graduating to their own dedicated repo. Each project subfolder should have its own README stating its target destination repo and current migration status.

## Current contents

- `excel-formatting/` — a VSTO/C# Excel add-in project (moved from `vba-excel/excel-formatting/` 2026-09-23). Will eventually become one function within a larger, separate Excel add-in repo (many functions, of which excel-formatting is one). Not yet moved out — still in progress.
