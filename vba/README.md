# VBA

True VBA modules (`.bas`/`.cls`/`.frm`) for Access, Excel, and Outlook — as opposed to `/vsto/`, which holds compiled VSTO (C#/VB.NET) add-in projects. Split by host application below.

Governance: see `/GOVERNANCE.md`, `/CONTRIBUTING.md`, `/TAGS.md`, `/AI-DISCLOSURE.md`, and `/LICENSE` at the repo root.
