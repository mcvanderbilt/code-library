# VSTO

Small, permanent VSTO (Visual Studio Tools for Office) helper projects that are NOT headed to their own repo — as opposed to `/staging/`, which holds larger VSTO/Office-add-in projects on their way out to a dedicated repo. Split by host application below.

Governance: see `/GOVERNANCE.md`, `/CONTRIBUTING.md`, `/TAGS.md`, `/AI-DISCLOSURE.md`, and `/LICENSE` at the repo root (pending — tracked in `BACKLOG.md`).
